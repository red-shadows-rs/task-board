### What does this PR do?

### Why?

Reference the `CHANGELOG.md` entry or issue this PR belongs to.

### How was it tested?

- [ ] `npm run validate` passes (format + lint + type-check)
- [ ] `npm run build` succeeds
- [ ] Manual smoke test on: <!-- Node version + browser -->
- [ ] Role permissions verified (leader / member / client) if API or permissions touched

### Checklist

- [ ] Bilingual: any new user-facing text has both `en` and `ar` locale keys.
- [ ] `CHANGELOG.md` updated with a new versioned entry.
- [ ] Version bump in `package.json` if this is a release PR.
